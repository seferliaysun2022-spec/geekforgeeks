class Stack:
    def __init__(self, capacity):
        self.arr = [None] * capacity
        self.capacity = capacity
        self.top = -1
    
    def push(self, x):
        if self.is_full():
            print("❌ Stack doludur! Əlavə edilə bilməz.")
            return False
        self.top += 1
        self.arr[self.top] = x
        print(f"✅ {x} əlavə edildi")
        return True
    
    def pop(self):
        if self.is_empty():
            print("❌ Stack boşdur! Silinəcək element yoxdur.")
            return None
        value = self.arr[self.top]
        self.top -= 1
        print(f"✅ {value} silindi")
        return value
    
    def peek(self):
        if self.is_empty():
            print("📭 Stack boşdur")
            return None
        return self.arr[self.top]
    
    def is_empty(self):
        return self.top == -1
    
    def is_full(self):
        return self.top == self.capacity - 1
    
    def show(self):
        if self.is_empty():
            print("📭 Stack boşdur")
        else:
            print("📊 Stack məlumatları:")
            print(f"   Tutum: {self.capacity}")
            print(f"   Cari ölçü: {self.top + 1}")
            print(f"   Üst element: {self.peek()}")
            print("   Elementlər (aşağıdan yuxarıya):")
            for i in range(self.top + 1):
                print(f"   [{i}] → {self.arr[i]}")



class DynamicStack:
    def __init__(self):
        self.arr = []
    
    def push(self, x):
        self.arr.append(x)
        print(f"✅ {x} əlavə edildi")
        return True
    
    def pop(self):
        if self.is_empty():
            print("❌ Stack boşdur! Silinəcək element yoxdur.")
            return None
        value = self.arr.pop()
        print(f"{value} silindi")
        return value
    
    def peek(self):
        if self.is_empty():
            print("📭 Stack boşdur")
            return None
        return self.arr[-1]
    
    def is_empty(self):
        return len(self.arr) == 0
    
    def size(self):
        return len(self.arr)
    
    def show(self):
        if self.is_empty():
            print(" Stack boşdur")
        else:
            print(" Stack məlumatları:")
            print(f"   Cari ölçü: {self.size()}")
            print(f"   Üst element: {self.peek()}")
            print("   Elementlər (aşağıdan yuxarıya):")
            for i, val in enumerate(self.arr):
                print(f"   [{i}] → {val}")


def main():
    print("=" * 50)
    print("📚 STACK İMPLEMENTASİYASI PROQRAMI")
    print("=" * 50)
    
    
    print("\nStack növünü seçin:")
    print("1. Sabit ölçülü Stack")
    print("2. Dinamik ölçülü Stack")
    
    choice = input("Seçiminiz (1/2): ")
    
    if choice == "1":
        
        while True:
            try:
                capacity = int(input("📏 Stack-in tutumunu daxil edin: "))
                if capacity > 0:
                    stack = Stack(capacity)
                    break
                else:
                    print("⚠️ Müsbət ədəd daxil edin!")
            except ValueError:
                print("⚠️ Rəqəm daxil edin!")
    else:
       
        stack = DynamicStack()
        capacity = "∞ (limitsiz)"
    
    print(f"\n✅ {capacity} tutumlu Stack yaradıldı!")
    
    
    while True:
        print("\n" + "=" * 40)
        print(" ƏMƏLİYYATLAR MENYUSU")
        print("=" * 40)
        print("1. Push (Element əlavə et)")
        print("2. Pop (Element sil)")
        print("3. Peek (Üst elementə bax)")
        print("4. Stack-i göstər")
        print("5. Boş olub-olmadığını yoxla")
        
        if choice == "1":
            print("6. Dolu olub-olmadığını yoxla")
            print("7. Proqramdan çıx")
        else:
            print("6. Ölçüyə bax")
            print("7. Proqramdan çıx")
        
        print("=" * 40)
        
        operation = input("Seçiminiz (1-7): ")
        
        if operation == "1":
            
            try:
                value = int(input("Əlavə ediləcək ədədi daxil edin: "))
                stack.push(value)
            except ValueError:
                print("⚠️ Rəqəm daxil edin!")
        
        elif operation == "2":
            
            result = stack.pop()
            if result is not None:
                print(f" Silinən element: {result}")
        
        elif operation == "3":
           
            result = stack.peek()
            if result is not None:
                print(f" Üst element: {result}")
        
        elif operation == "4":
            
            stack.show()
        
        elif operation == "5":
            
            if stack.is_empty():
                print("Stack BOŞDUR")
            else:
                print(" Stack BOŞ DEYİL")
        
        elif operation == "6":
            if choice == "1":
                
                if stack.is_full():
                    print("✅ Stack DOLUDUR")
                else:
                    print("✅ Stack DOLU DEYİL")
            else:
                
                print(f"📏 Stack ölçüsü: {stack.size()}")
        
        elif operation == "7":
            
            print("\n👋 Proqramdan çıxılır...")
            print("Təşəkkürlər!")
            break
        
        else:
            print("⚠️ Yanlış seçim! 1-7 arası rəqəm daxil edin.")
        
     
        if operation != "7":
            input("\n⏎ Davam etmək üçün Enter düyməsini basın")

if __name__ == "__main__":
    main()