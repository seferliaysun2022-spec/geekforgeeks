from collections import deque

class DequeStack:
    def __init__(self):
        self.stack = deque()
    
    def push(self, value):
        """Element əlavə et (push)"""
        self.stack.append(value)
        print(f"✅ {value} stack-ə əlavə edildi")
    
    def pop(self):
        """Element sil (pop)"""
        if self.is_empty():
            print("❌ Stack boşdur!")
            return None
        
        value = self.stack.pop()
        print(f"✅ {value} stack-dən silindi")
        return value
    
    def peek(self):
        """Üst elementə bax"""
        if self.is_empty():
            print("📭 Stack boşdur")
            return None
        return self.stack[-1]
    
    def is_empty(self):
        """Boş olub-olmadığını yoxla"""
        return len(self.stack) == 0
    
    def size(self):
        """Element sayını qaytar"""
        return len(self.stack)
    
    def display(self):
        """Stack-i göstər"""
        if self.is_empty():
            print("📭 Stack boşdur")
            return
        
        print("📊 Stack məlumatları:")
        print(f"   Element sayı: {self.size()}")
        print(f"   Üst element: {self.peek()}")
        print("   Elementlər (aşağıdan yuxarıya):")
        
        # List kimi çap et (əks istiqamətdə)
        for i in range(len(self.stack)-1, -1, -1):
            print(f"   [{i}] → {self.stack[i]}")


def main():
    print("=" * 50)
    print("🔄 DEQUE İLƏ STACK PROQRAMI")
    print("=" * 50)
    
    stack = DequeStack()
    
    while True:
        print("\n" + "=" * 40)
        print("📋 ƏMƏLİYYATLAR MENYUSU")
        print("=" * 40)
        print("1. Push (Element əlavə et)")
        print("2. Pop (Element sil)")
        print("3. Peek (Üst elementə bax)")
        print("4. Stack-i göstər")
        print("5. Boş olub-olmadığını yoxla")
        print("6. Element sayını göstər")
        print("7. Bütün elementləri sil (Clear)")
        print("8. Proqramdan çıx")
        print("=" * 40)
        
        choice = input("Seçiminiz (1-8): ")
        
        if choice == "1":
            try:
                value = int(input("Əlavə ediləcək ədədi daxil edin: "))
                stack.push(value)
            except ValueError:
                print("⚠️ Rəqəm daxil edin!")
        
        elif choice == "2":
            result = stack.pop()
            if result is not None:
                print(f"📤 Silinən element: {result}")
        
        elif choice == "3":
            result = stack.peek()
            if result is not None:
                print(f"👆 Üst element: {result}")
        
        elif choice == "4":
            stack.display()
        
        elif choice == "5":
            if stack.is_empty():
                print("✅ Stack BOŞDUR")
            else:
                print("✅ Stack BOŞ DEYİL")
        
        elif choice == "6":
            print(f"📏 Stack-dəki element sayı: {stack.size()}")
        
        elif choice == "7":
            if not stack.is_empty():
                print("🧹 Bütün elementlər silinir...")
                while not stack.is_empty():
                    stack.pop()
                print("✅ Stack tamamilə təmizləndi")
            else:
                print("📭 Stack artıq boşdur")
        
        elif choice == "8":
            print("\n👋 Proqramdan çıxılır...")
            print("Təşəkkürlər!")
            break
        
        else:
            print("⚠️ Yanlış seçim! 1-8 arası rəqəm daxil edin.")
        
        if choice != "8":
            input("\n⏎ Davam etmək üçün Enter düyməsini basın...")



if __name__ == "__main__":
    main()